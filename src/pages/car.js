import React, { Component } from 'react';
import Tabbar from '../components/tabbar';

class Car extends Component {
  render() {
    return (
      <div>
        Car
        <Tabbar />
      </div>
    );
  }
}

export default Car;
